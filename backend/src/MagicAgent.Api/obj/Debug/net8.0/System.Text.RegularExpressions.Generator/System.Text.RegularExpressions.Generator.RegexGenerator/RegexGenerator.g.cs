// <auto-generated/>
#nullable enable
#pragma warning disable CS0162 // Unreachable code
#pragma warning disable CS0164 // Unreferenced label
#pragma warning disable CS0219 // Variable assigned but never used

namespace ModelContextProtocol.Client
{
    partial class StdioClientTransport
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>[&amp;^&gt;&lt;|]</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.CultureInvariant</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a character in the set [&amp;&lt;&gt;^|].<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex GetWindowsCliSpecialArgumentsRegex() => global::System.Text.RegularExpressions.Generated.GetWindowsCliSpecialArgumentsRegex_0.Instance;
    }
}

namespace ModelContextProtocol.Client
{
    partial class StdioClientTransport
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>\\s</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.CultureInvariant</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a whitespace character.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex GetContainsWhitespaceRegex() => global::System.Text.RegularExpressions.Generated.GetContainsWhitespaceRegex_1.Instance;
    }
}

namespace ModelContextProtocol.Client
{
    partial class StdioClientTransport
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>[\\s\\.]+</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a character in the set [.\s] atomically at least once.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex WhitespaceAndPeriods() => global::System.Text.RegularExpressions.Generated.WhitespaceAndPeriods_2.Instance;
    }
}

namespace ModelContextProtocol.Server
{
    partial class AIFunctionMcpServerTool
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>[^0-9A-Za-z]+</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a character in the set [^0-9A-Za-z] atomically at least once.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex NonAsciiLetterDigitsRegex() => global::System.Text.RegularExpressions.Generated.NonAsciiLetterDigitsRegex_3.Instance;
    }
}

namespace ModelContextProtocol.Server
{
    partial class AIFunctionMcpServerTool
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[A-Za-z0-9_.-]{1,128}\\z</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match a character in the set [-.0-9A-Z_a-z] atomically at least 1 and at most 128 times.<br/>
        /// ○ Match if at the end of the string.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex ValidateToolNameRegex() => global::System.Text.RegularExpressions.Generated.ValidateToolNameRegex_4.Instance;
    }
}

namespace ModelContextProtocol
{
    partial class UriTemplate
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>{                                                       # opening brace\n    (?&lt;operator&gt;[+#./;?&amp;]?)                             # optional operator\n    (?&lt;varname&gt;\n        (?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2})                # varchar: letter, digit, underscore, or pct-encoded\n        (?:\\.?(?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2}))*        # optionally dot-separated subsequent varchars\n    )\n    (?: :[1-9][0-9]{0,3} )?                             # optional prefix modifier (1–4 digits)\n    \\*?                                                 # optional explode\n    (?:,                                                # comma separator, followed by the same as above\n        (?&lt;varname&gt;\n            (?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2})\n            (?:\\.?(?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2}))*\n        )\n        (?: :[1-9][0-9]{0,3} )?\n        \\*?\n    )*                                                  # zero or more additional vars\n}                                                       # closing brace</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.IgnorePatternWhitespace</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match '{'.<br/>
        /// ○ "operator" capture group.<br/>
        ///     ○ Match a character in the set [#&amp;+./;?] atomically, optionally.<br/>
        /// ○ "varname" capture group.<br/>
        ///     ○ Match with 2 alternative expressions.<br/>
        ///         ○ Match a character in the set [0-9A-Z_a-z].<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match '%'.<br/>
        ///             ○ Match a character in the set [0-9A-Fa-f] exactly 2 times.<br/>
        ///     ○ Loop greedily any number of times.<br/>
        ///         ○ Match '.' atomically, optionally.<br/>
        ///         ○ Match with 2 alternative expressions.<br/>
        ///             ○ Match a character in the set [0-9A-Z_a-z].<br/>
        ///             ○ Match a sequence of expressions.<br/>
        ///                 ○ Match '%'.<br/>
        ///                 ○ Match a character in the set [0-9A-Fa-f] exactly 2 times.<br/>
        /// ○ Optional (greedy).<br/>
        ///     ○ Match ':'.<br/>
        ///     ○ Match a character in the set [1-9].<br/>
        ///     ○ Match a character in the set [0-9] greedily at most 3 times.<br/>
        /// ○ Match '*' greedily, optionally.<br/>
        /// ○ Loop greedily any number of times.<br/>
        ///     ○ Match ','.<br/>
        ///     ○ "varname" capture group.<br/>
        ///         ○ Match with 2 alternative expressions.<br/>
        ///             ○ Match a character in the set [0-9A-Z_a-z].<br/>
        ///             ○ Match a sequence of expressions.<br/>
        ///                 ○ Match '%'.<br/>
        ///                 ○ Match a character in the set [0-9A-Fa-f] exactly 2 times.<br/>
        ///         ○ Loop greedily any number of times.<br/>
        ///             ○ Match '.' atomically, optionally.<br/>
        ///             ○ Match with 2 alternative expressions.<br/>
        ///                 ○ Match a character in the set [0-9A-Z_a-z].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match '%'.<br/>
        ///                     ○ Match a character in the set [0-9A-Fa-f] exactly 2 times.<br/>
        ///     ○ Optional (greedy).<br/>
        ///         ○ Match ':'.<br/>
        ///         ○ Match a character in the set [1-9].<br/>
        ///         ○ Match a character in the set [0-9] greedily at most 3 times.<br/>
        ///     ○ Match '*' atomically, optionally.<br/>
        /// ○ Match '}'.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
        private static partial global::System.Text.RegularExpressions.Regex UriTemplateExpression() => global::System.Text.RegularExpressions.Generated.UriTemplateExpression_5.Instance;
    }
}

namespace System.Text.RegularExpressions.Generated
{
    using System;
    using System.Buffers;
    using System.CodeDom.Compiler;
    using System.Collections;
    using System.ComponentModel;
    using System.Globalization;
    using System.Runtime.CompilerServices;
    using System.Text.RegularExpressions;
    using System.Threading;

    /// <summary>Custom <see cref="Regex"/>-derived type for the GetWindowsCliSpecialArgumentsRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class GetWindowsCliSpecialArgumentsRegex_0 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly GetWindowsCliSpecialArgumentsRegex_0 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private GetWindowsCliSpecialArgumentsRegex_0()
        {
            base.pattern = "[&^><|]";
            base.roptions = RegexOptions.CultureInvariant;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    if (TryFindNextPossibleStartingPosition(inputSpan))
                    {
                        // The search in TryFindNextPossibleStartingPosition performed the entire match.
                        int start = base.runtextpos;
                        int end = base.runtextpos = start + 1;
                        base.Capture(0, start, end);
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern begins with a character in the set [&<>^|].
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfAny(Utilities.s_ascii_400000500000004000000010);
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the GetContainsWhitespaceRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class GetContainsWhitespaceRegex_1 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly GetContainsWhitespaceRegex_1 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private GetContainsWhitespaceRegex_1()
        {
            base.pattern = "\\s";
            base.roptions = RegexOptions.CultureInvariant;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    if (TryFindNextPossibleStartingPosition(inputSpan))
                    {
                        // The search in TryFindNextPossibleStartingPosition performed the entire match.
                        int start = base.runtextpos;
                        int end = base.runtextpos = start + 1;
                        base.Capture(0, start, end);
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern begins with a whitespace character.
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfAnyWhiteSpace();
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the WhitespaceAndPeriods method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class WhitespaceAndPeriods_2 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly WhitespaceAndPeriods_2 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private WhitespaceAndPeriods_2()
        {
            base.pattern = "[\\s\\.]+";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern begins with a character in the set [.\s].
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfNonAsciiOrAny_38C8765B391EE29A127A852BF93BF6281EC129E7F4C5EBA9284D6322E5BD2D52();
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match a character in the set [.\s] atomically at least once.
                    {
                        int iteration = 0;
                        while ((uint)iteration < (uint)slice.Length && ((ch = slice[iteration]) < 128 ? ("㸀\0䀁\0\0\0\0\0"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\u0002\u0001./d")))
                        {
                            iteration++;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the NonAsciiLetterDigitsRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class NonAsciiLetterDigitsRegex_3 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly NonAsciiLetterDigitsRegex_3 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private NonAsciiLetterDigitsRegex_3()
        {
            base.pattern = "[^0-9A-Za-z]+";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern begins with a character in the set [^0-9A-Za-z].
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfAnyExcept(Utilities.s_asciiLettersAndDigits);
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match a character in the set [^0-9A-Za-z] atomically at least once.
                    {
                        int iteration = slice.IndexOfAny(Utilities.s_asciiLettersAndDigits);
                        if (iteration < 0)
                        {
                            iteration = slice.Length;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the ValidateToolNameRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class ValidateToolNameRegex_4 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly ValidateToolNameRegex_4 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private ValidateToolNameRegex_4()
        {
            base.pattern = "^[A-Za-z0-9_.-]{1,128}\\z";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match a character in the set [-.0-9A-Z_a-z] atomically at least 1 and at most 128 times.
                    {
                        int iteration = 0;
                        while (iteration < 128 && (uint)iteration < (uint)slice.Length && ((ch = slice[iteration]) < '{' && ("\0\0怀Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) != 0))
                        {
                            iteration++;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                    }
                    
                    // Match if at the end of the string.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the UriTemplateExpression method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    [SkipLocalsInit]
    file sealed class UriTemplateExpression_5 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly UriTemplateExpression_5 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private UriTemplateExpression_5()
        {
            base.pattern = "{                                                       # opening brace\n    (?<operator>[+#./;?&]?)                             # optional operator\n    (?<varname>\n        (?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2})                # varchar: letter, digit, underscore, or pct-encoded\n        (?:\\.?(?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2}))*        # optionally dot-separated subsequent varchars\n    )\n    (?: :[1-9][0-9]{0,3} )?                             # optional prefix modifier (1–4 digits)\n    \\*?                                                 # optional explode\n    (?:,                                                # comma separator, followed by the same as above\n        (?<varname>\n            (?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2})\n            (?:\\.?(?:[A-Za-z0-9_]|%[0-9A-Fa-f]{2}))*\n        )\n        (?: :[1-9][0-9]{0,3} )?\n        \\*?\n    )*                                                  # zero or more additional vars\n}                                                       # closing brace";
            base.roptions = RegexOptions.IgnorePatternWhitespace;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.CapNames = new Hashtable { { "0", 0 } ,  { "operator", 1 } ,  { "varname", 2 }  };
            base.capslist = new string[] {"0", "operator", "varname" };
            base.capsize = 3;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 3 characters.
                    if (pos <= inputSpan.Length - 3)
                    {
                        // The pattern begins with a character in the set {.
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOf('{');
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int alternation_branch = 0;
                    int alternation_starting_capturepos = 0;
                    int alternation_starting_pos = 0;
                    int capture_starting_pos = 0;
                    int capture_starting_pos1 = 0;
                    int charloop_capture_pos = 0;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    int charloop_starting_pos1 = 0, charloop_ending_pos1 = 0;
                    int charloop_starting_pos2 = 0, charloop_ending_pos2 = 0;
                    int loop_iteration = 0;
                    int loop_iteration1 = 0;
                    int loop_iteration2 = 0;
                    int loop_iteration3 = 0;
                    int loop_iteration4 = 0;
                    int stackpos = 0;
                    uint charMinusLowUInt32;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match '{'.
                    if (slice.IsEmpty || slice[0] != '{')
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // "operator" capture group.
                    {
                        pos++;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos = pos;
                        
                        // Match a character in the set [#&+./;?] atomically, optionally.
                        {
                            if (!slice.IsEmpty && ((int)((0x90980088U << (short)(charMinusLowUInt32 = (ushort)(slice[0] - '#'))) & (charMinusLowUInt32 - 32)) < 0))
                            {
                                slice = slice.Slice(1);
                                pos++;
                            }
                        }
                        
                        base.Capture(1, capture_starting_pos, pos);
                    }
                    
                    // "varname" capture group.
                    //{
                        capture_starting_pos1 = pos;
                        
                        // Match with 2 alternative expressions.
                        //{
                            alternation_starting_pos = pos;
                            alternation_starting_capturepos = base.Crawlpos();
                            
                            // Branch 0
                            //{
                                // Match a character in the set [0-9A-Z_a-z].
                                if (slice.IsEmpty || ((ch = slice[0]) >= '{' || ("\0\0\0Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) == 0))
                                {
                                    goto AlternationBranch;
                                }
                                
                                alternation_branch = 0;
                                pos++;
                                slice = inputSpan.Slice(pos);
                                goto AlternationMatch;
                                
                                AlternationBranch:
                                pos = alternation_starting_pos;
                                slice = inputSpan.Slice(pos);
                                UncaptureUntil(alternation_starting_capturepos);
                            //}
                            
                            // Branch 1
                            //{
                                if ((uint)slice.Length < 3 ||
                                    slice[0] != '%' || // Match '%'.
                                    !char.IsAsciiHexDigit(slice[1]) || // Match a character in the set [0-9A-Fa-f] exactly 2 times.
                                    !char.IsAsciiHexDigit(slice[2]))
                                {
                                    UncaptureUntil(0);
                                    return false; // The input didn't match.
                                }
                                
                                alternation_branch = 1;
                                pos += 3;
                                slice = inputSpan.Slice(pos);
                                goto AlternationMatch;
                            //}
                            
                            AlternationBacktrack:
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            switch (alternation_branch)
                            {
                                case 0:
                                    goto AlternationBranch;
                                case 1:
                                    UncaptureUntil(0);
                                    return false; // The input didn't match.
                            }
                            
                            AlternationMatch:;
                        //}
                        
                        // Loop greedily any number of times.
                        //{
                            loop_iteration = 0;
                            
                            LoopBody:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                            
                            loop_iteration++;
                            
                            // Match '.' atomically, optionally.
                            {
                                if (!slice.IsEmpty && slice[0] == '.')
                                {
                                    slice = slice.Slice(1);
                                    pos++;
                                }
                            }
                            
                            // Match with 2 alternative expressions.
                            //{
                                int alternation_starting_pos1 = pos;
                                int alternation_starting_capturepos1 = base.Crawlpos();
                                
                                // Branch 0
                                //{
                                    // Match a character in the set [0-9A-Z_a-z].
                                    if (slice.IsEmpty || ((ch = slice[0]) >= '{' || ("\0\0\0Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) == 0))
                                    {
                                        goto AlternationBranch1;
                                    }
                                    
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos1, alternation_starting_capturepos1);
                                    pos++;
                                    slice = inputSpan.Slice(pos);
                                    goto AlternationMatch1;
                                    
                                    AlternationBranch1:
                                    pos = alternation_starting_pos1;
                                    slice = inputSpan.Slice(pos);
                                    UncaptureUntil(alternation_starting_capturepos1);
                                //}
                                
                                // Branch 1
                                //{
                                    if ((uint)slice.Length < 3 ||
                                        slice[0] != '%' || // Match '%'.
                                        !char.IsAsciiHexDigit(slice[1]) || // Match a character in the set [0-9A-Fa-f] exactly 2 times.
                                        !char.IsAsciiHexDigit(slice[2]))
                                    {
                                        goto LoopIterationNoMatch;
                                    }
                                    
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos1, alternation_starting_capturepos1);
                                    pos += 3;
                                    slice = inputSpan.Slice(pos);
                                    goto AlternationMatch1;
                                //}
                                
                                AlternationBacktrack1:
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                Utilities.StackPop(base.runstack!, ref stackpos, out alternation_starting_capturepos1, out alternation_starting_pos1);
                                switch (base.runstack![--stackpos])
                                {
                                    case 0:
                                        goto AlternationBranch1;
                                    case 1:
                                        goto LoopIterationNoMatch;
                                }
                                
                                AlternationMatch1:;
                            //}
                            
                            
                            // The loop has no upper bound. Continue iterating greedily.
                            goto LoopBody;
                            
                            // The loop iteration failed. Put state back to the way it was before the iteration.
                            LoopIterationNoMatch:
                            if (--loop_iteration < 0)
                            {
                                // Unable to match the remainder of the expression after exhausting the loop.
                                goto AlternationBacktrack;
                            }
                            pos = base.runstack![--stackpos];
                            UncaptureUntil(base.runstack![--stackpos]);
                            slice = inputSpan.Slice(pos);
                            goto LoopEnd;
                            
                            LoopBacktrack:
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (loop_iteration == 0)
                            {
                                // No iterations of the loop remain to backtrack into. Fail the loop.
                                goto AlternationBacktrack;
                            }
                            goto AlternationBacktrack1;
                            LoopEnd:;
                        //}
                        
                        base.Capture(2, capture_starting_pos1, pos);
                        
                        goto CaptureSkipBacktrack;
                        
                        CaptureBacktrack:
                        goto LoopBacktrack;
                        
                        CaptureSkipBacktrack:;
                    //}
                    
                    // Optional (greedy).
                    //{
                        loop_iteration1 = 0;
                        
                        LoopBody1:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                        
                        loop_iteration1++;
                        
                        if ((uint)slice.Length < 2 ||
                            slice[0] != ':' || // Match ':'.
                            !char.IsBetween(slice[1], '1', '9')) // Match a character in the set [1-9].
                        {
                            goto LoopIterationNoMatch1;
                        }
                        
                        // Match a character in the set [0-9] greedily at most 3 times.
                        //{
                            pos += 2;
                            slice = inputSpan.Slice(pos);
                            charloop_starting_pos = pos;
                            
                            int iteration = 0;
                            while (iteration < 3 && (uint)iteration < (uint)slice.Length && char.IsAsciiDigit(slice[iteration]))
                            {
                                iteration++;
                            }
                            
                            slice = slice.Slice(iteration);
                            pos += iteration;
                            
                            charloop_ending_pos = pos;
                            goto CharLoopEnd;
                            
                            CharLoopBacktrack:
                            UncaptureUntil(base.runstack![--stackpos]);
                            Utilities.StackPop(base.runstack!, ref stackpos, out charloop_ending_pos, out charloop_starting_pos);
                            
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (charloop_starting_pos >= charloop_ending_pos)
                            {
                                goto LoopIterationNoMatch1;
                            }
                            pos = --charloop_ending_pos;
                            slice = inputSpan.Slice(pos);
                            
                            CharLoopEnd:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, charloop_starting_pos, charloop_ending_pos, base.Crawlpos());
                        //}
                        
                        
                        // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                        if (loop_iteration1 == 0)
                        {
                            goto LoopBody1;
                        }
                        goto LoopEnd1;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch1:
                        if (--loop_iteration1 < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto CaptureBacktrack;
                        }
                        pos = base.runstack![--stackpos];
                        UncaptureUntil(base.runstack![--stackpos]);
                        slice = inputSpan.Slice(pos);
                        goto LoopEnd1;
                        
                        LoopBacktrack1:
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (loop_iteration1 == 0)
                        {
                            // No iterations of the loop remain to backtrack into. Fail the loop.
                            goto CaptureBacktrack;
                        }
                        goto CharLoopBacktrack;
                        LoopEnd1:;
                    //}
                    
                    // Match '*' greedily, optionally.
                    //{
                        charloop_starting_pos1 = pos;
                        
                        if (!slice.IsEmpty && slice[0] == '*')
                        {
                            slice = slice.Slice(1);
                            pos++;
                        }
                        
                        charloop_ending_pos1 = pos;
                        goto CharLoopEnd1;
                        
                        CharLoopBacktrack1:
                        UncaptureUntil(charloop_capture_pos);
                        
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (charloop_starting_pos1 >= charloop_ending_pos1)
                        {
                            goto LoopBacktrack1;
                        }
                        pos = --charloop_ending_pos1;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd1:
                        charloop_capture_pos = base.Crawlpos();
                    //}
                    
                    // Loop greedily any number of times.
                    //{
                        loop_iteration2 = 0;
                        
                        LoopBody2:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                        
                        loop_iteration2++;
                        
                        // Match ','.
                        if (slice.IsEmpty || slice[0] != ',')
                        {
                            goto LoopIterationNoMatch2;
                        }
                        
                        // "varname" capture group.
                        //{
                            pos++;
                            slice = inputSpan.Slice(pos);
                            int capture_starting_pos2 = pos;
                            
                            // Match with 2 alternative expressions.
                            //{
                                int alternation_starting_pos2 = pos;
                                int alternation_starting_capturepos2 = base.Crawlpos();
                                
                                // Branch 0
                                //{
                                    // Match a character in the set [0-9A-Z_a-z].
                                    if (slice.IsEmpty || ((ch = slice[0]) >= '{' || ("\0\0\0Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) == 0))
                                    {
                                        goto AlternationBranch2;
                                    }
                                    
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos2, alternation_starting_capturepos2);
                                    pos++;
                                    slice = inputSpan.Slice(pos);
                                    goto AlternationMatch2;
                                    
                                    AlternationBranch2:
                                    pos = alternation_starting_pos2;
                                    slice = inputSpan.Slice(pos);
                                    UncaptureUntil(alternation_starting_capturepos2);
                                //}
                                
                                // Branch 1
                                //{
                                    if ((uint)slice.Length < 3 ||
                                        slice[0] != '%' || // Match '%'.
                                        !char.IsAsciiHexDigit(slice[1]) || // Match a character in the set [0-9A-Fa-f] exactly 2 times.
                                        !char.IsAsciiHexDigit(slice[2]))
                                    {
                                        goto LoopIterationNoMatch2;
                                    }
                                    
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos2, alternation_starting_capturepos2);
                                    pos += 3;
                                    slice = inputSpan.Slice(pos);
                                    goto AlternationMatch2;
                                //}
                                
                                AlternationBacktrack2:
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                Utilities.StackPop(base.runstack!, ref stackpos, out alternation_starting_capturepos2, out alternation_starting_pos2);
                                switch (base.runstack![--stackpos])
                                {
                                    case 0:
                                        goto AlternationBranch2;
                                    case 1:
                                        goto LoopIterationNoMatch2;
                                }
                                
                                AlternationMatch2:;
                            //}
                            
                            // Loop greedily any number of times.
                            //{
                                loop_iteration3 = 0;
                                
                                LoopBody3:
                                Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                                
                                loop_iteration3++;
                                
                                // Match '.' atomically, optionally.
                                {
                                    if (!slice.IsEmpty && slice[0] == '.')
                                    {
                                        slice = slice.Slice(1);
                                        pos++;
                                    }
                                }
                                
                                // Match with 2 alternative expressions.
                                //{
                                    int alternation_starting_pos3 = pos;
                                    int alternation_starting_capturepos3 = base.Crawlpos();
                                    
                                    // Branch 0
                                    //{
                                        // Match a character in the set [0-9A-Z_a-z].
                                        if (slice.IsEmpty || ((ch = slice[0]) >= '{' || ("\0\0\0Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) == 0))
                                        {
                                            goto AlternationBranch3;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos3, alternation_starting_capturepos3);
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch3;
                                        
                                        AlternationBranch3:
                                        pos = alternation_starting_pos3;
                                        slice = inputSpan.Slice(pos);
                                        UncaptureUntil(alternation_starting_capturepos3);
                                    //}
                                    
                                    // Branch 1
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            slice[0] != '%' || // Match '%'.
                                            !char.IsAsciiHexDigit(slice[1]) || // Match a character in the set [0-9A-Fa-f] exactly 2 times.
                                            !char.IsAsciiHexDigit(slice[2]))
                                        {
                                            goto LoopIterationNoMatch3;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos3, alternation_starting_capturepos3);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch3;
                                    //}
                                    
                                    AlternationBacktrack3:
                                    if (Utilities.s_hasTimeout)
                                    {
                                        base.CheckTimeout();
                                    }
                                    
                                    Utilities.StackPop(base.runstack!, ref stackpos, out alternation_starting_capturepos3, out alternation_starting_pos3);
                                    switch (base.runstack![--stackpos])
                                    {
                                        case 0:
                                            goto AlternationBranch3;
                                        case 1:
                                            goto LoopIterationNoMatch3;
                                    }
                                    
                                    AlternationMatch3:;
                                //}
                                
                                
                                // The loop has no upper bound. Continue iterating greedily.
                                goto LoopBody3;
                                
                                // The loop iteration failed. Put state back to the way it was before the iteration.
                                LoopIterationNoMatch3:
                                if (--loop_iteration3 < 0)
                                {
                                    // Unable to match the remainder of the expression after exhausting the loop.
                                    goto AlternationBacktrack2;
                                }
                                pos = base.runstack![--stackpos];
                                UncaptureUntil(base.runstack![--stackpos]);
                                slice = inputSpan.Slice(pos);
                                goto LoopEnd3;
                                
                                LoopBacktrack2:
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                if (loop_iteration3 == 0)
                                {
                                    // No iterations of the loop remain to backtrack into. Fail the loop.
                                    goto AlternationBacktrack2;
                                }
                                goto AlternationBacktrack3;
                                LoopEnd3:
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, loop_iteration3);
                                goto LoopSkipBacktrack;
                                
                                LoopBacktrack3:
                                loop_iteration3 = base.runstack![--stackpos];
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                goto LoopBacktrack2;
                                
                                LoopSkipBacktrack:;
                            //}
                            
                            base.Capture(2, capture_starting_pos2, pos);
                            
                            Utilities.StackPush(ref base.runstack!, ref stackpos, capture_starting_pos2);
                            goto CaptureSkipBacktrack1;
                            
                            CaptureBacktrack1:
                            capture_starting_pos2 = base.runstack![--stackpos];
                            goto LoopBacktrack3;
                            
                            CaptureSkipBacktrack1:;
                        //}
                        
                        // Optional (greedy).
                        //{
                            loop_iteration4 = 0;
                            
                            LoopBody4:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                            
                            loop_iteration4++;
                            
                            if ((uint)slice.Length < 2 ||
                                slice[0] != ':' || // Match ':'.
                                !char.IsBetween(slice[1], '1', '9')) // Match a character in the set [1-9].
                            {
                                goto LoopIterationNoMatch4;
                            }
                            
                            // Match a character in the set [0-9] greedily at most 3 times.
                            //{
                                pos += 2;
                                slice = inputSpan.Slice(pos);
                                charloop_starting_pos2 = pos;
                                
                                int iteration1 = 0;
                                while (iteration1 < 3 && (uint)iteration1 < (uint)slice.Length && char.IsAsciiDigit(slice[iteration1]))
                                {
                                    iteration1++;
                                }
                                
                                slice = slice.Slice(iteration1);
                                pos += iteration1;
                                
                                charloop_ending_pos2 = pos;
                                goto CharLoopEnd2;
                                
                                CharLoopBacktrack2:
                                UncaptureUntil(base.runstack![--stackpos]);
                                Utilities.StackPop(base.runstack!, ref stackpos, out charloop_ending_pos2, out charloop_starting_pos2);
                                
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                if (charloop_starting_pos2 >= charloop_ending_pos2)
                                {
                                    goto LoopIterationNoMatch4;
                                }
                                pos = --charloop_ending_pos2;
                                slice = inputSpan.Slice(pos);
                                
                                CharLoopEnd2:
                                Utilities.StackPush(ref base.runstack!, ref stackpos, charloop_starting_pos2, charloop_ending_pos2, base.Crawlpos());
                            //}
                            
                            
                            // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                            if (loop_iteration4 == 0)
                            {
                                goto LoopBody4;
                            }
                            goto LoopEnd4;
                            
                            // The loop iteration failed. Put state back to the way it was before the iteration.
                            LoopIterationNoMatch4:
                            if (--loop_iteration4 < 0)
                            {
                                // Unable to match the remainder of the expression after exhausting the loop.
                                goto CaptureBacktrack1;
                            }
                            pos = base.runstack![--stackpos];
                            UncaptureUntil(base.runstack![--stackpos]);
                            slice = inputSpan.Slice(pos);
                            goto LoopEnd4;
                            
                            LoopBacktrack4:
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (loop_iteration4 == 0)
                            {
                                // No iterations of the loop remain to backtrack into. Fail the loop.
                                goto CaptureBacktrack1;
                            }
                            goto CharLoopBacktrack2;
                            LoopEnd4:
                            
                            Utilities.StackPush(ref base.runstack!, ref stackpos, loop_iteration4);
                            goto LoopSkipBacktrack1;
                            
                            LoopBacktrack5:
                            loop_iteration4 = base.runstack![--stackpos];
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            goto LoopBacktrack4;
                            
                            LoopSkipBacktrack1:;
                        //}
                        
                        // Match '*' atomically, optionally.
                        {
                            if (!slice.IsEmpty && slice[0] == '*')
                            {
                                slice = slice.Slice(1);
                                pos++;
                            }
                        }
                        
                        
                        // The loop has no upper bound. Continue iterating greedily.
                        goto LoopBody2;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch2:
                        if (--loop_iteration2 < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto CharLoopBacktrack1;
                        }
                        pos = base.runstack![--stackpos];
                        UncaptureUntil(base.runstack![--stackpos]);
                        slice = inputSpan.Slice(pos);
                        goto LoopEnd2;
                        
                        LoopBacktrack6:
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (loop_iteration2 == 0)
                        {
                            // No iterations of the loop remain to backtrack into. Fail the loop.
                            goto CharLoopBacktrack1;
                        }
                        goto LoopBacktrack5;
                        LoopEnd2:;
                    //}
                    
                    // Match '}'.
                    if (slice.IsEmpty || slice[0] != '}')
                    {
                        goto LoopBacktrack6;
                    }
                    
                    // The input matched.
                    pos++;
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                    
                    // <summary>Undo captures until it reaches the specified capture position.</summary>
                    [MethodImpl(MethodImplOptions.AggressiveInlining)]
                    void UncaptureUntil(int capturePosition)
                    {
                        while (base.Crawlpos() > capturePosition)
                        {
                            base.Uncapture();
                        }
                    }
                }
            }
        }

    }
    
    /// <summary>Helper methods used by generated <see cref="Regex"/>-derived implementations.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.12.47513")]
    file static class Utilities
    {
        /// <summary>Default timeout value set in <see cref="AppContext"/>, or <see cref="Regex.InfiniteMatchTimeout"/> if none was set.</summary>
        internal static readonly TimeSpan s_defaultTimeout = AppContext.GetData("REGEX_DEFAULT_MATCH_TIMEOUT") is TimeSpan timeout ? timeout : Regex.InfiniteMatchTimeout;
        
        /// <summary>Whether <see cref="s_defaultTimeout"/> is non-infinite.</summary>
        internal static readonly bool s_hasTimeout = s_defaultTimeout != Regex.InfiniteMatchTimeout;
        
        /// <summary>Finds the next index of any character that matches a whitespace character.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static int IndexOfAnyWhiteSpace(this ReadOnlySpan<char> span)
        {
            int i = span.IndexOfAnyExcept(Utilities.s_asciiExceptWhiteSpace);
            if ((uint)i < (uint)span.Length)
            {
                if (char.IsAscii(span[i]))
                {
                    return i;
                }
        
                do
                {
                    if (char.IsWhiteSpace(span[i]))
                    {
                        return i;
                    }
                    i++;
                }
                while ((uint)i < (uint)span.Length);
            }
        
            return -1;
        }
        
        /// <summary>Finds the next index of any character that matches a character in the set [.\s].</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static int IndexOfNonAsciiOrAny_38C8765B391EE29A127A852BF93BF6281EC129E7F4C5EBA9284D6322E5BD2D52(this ReadOnlySpan<char> span)
        {
            int i = span.IndexOfAnyExcept(Utilities.s_ascii_FFC1FFFFFEBFFFFFFFFFFFFFFFFFFFFF);
            if ((uint)i < (uint)span.Length)
            {
                if (char.IsAscii(span[i]))
                {
                    return i;
                }
        
                char ch;
                do
                {
                    if (((ch = span[i]) < 128 ? ("㸀\0䀁\0\0\0\0\0"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\u0002\u0001./d")))
                    {
                        return i;
                    }
                    i++;
                }
                while ((uint)i < (uint)span.Length);
            }
        
            return -1;
        }
        
        /// <summary>Pops 2 values from the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPop(int[] stack, ref int pos, out int arg0, out int arg1)
        {
            arg0 = stack[--pos];
            arg1 = stack[--pos];
        }
        
        /// <summary>Pushes 1 value onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0)
        {
            // If there's space available for the value, store it.
            int[] s = stack;
            int p = pos;
            if ((uint)p < (uint)s.Length)
            {
                s[p] = arg0;
                pos++;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0);
        
            // <summary>Resize the backtracking stack array and push 1 value onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0)
            {
                Array.Resize(ref stack, (pos + 0) * 2);
                StackPush(ref stack, ref pos, arg0);
            }
        }
        
        /// <summary>Pushes 2 values onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0, int arg1)
        {
            // If there's space available for all 2 values, store them.
            int[] s = stack;
            int p = pos;
            if ((uint)(p + 1) < (uint)s.Length)
            {
                s[p] = arg0;
                s[p + 1] = arg1;
                pos += 2;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0, arg1);
        
            // <summary>Resize the backtracking stack array and push 2 values onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0, int arg1)
            {
                Array.Resize(ref stack, (pos + 1) * 2);
                StackPush(ref stack, ref pos, arg0, arg1);
            }
        }
        
        /// <summary>Pushes 3 values onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0, int arg1, int arg2)
        {
            // If there's space available for all 3 values, store them.
            int[] s = stack;
            int p = pos;
            if ((uint)(p + 2) < (uint)s.Length)
            {
                s[p] = arg0;
                s[p + 1] = arg1;
                s[p + 2] = arg2;
                pos += 3;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0, arg1, arg2);
        
            // <summary>Resize the backtracking stack array and push 3 values onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0, int arg1, int arg2)
            {
                Array.Resize(ref stack, (pos + 2) * 2);
                StackPush(ref stack, ref pos, arg0, arg1, arg2);
            }
        }
        
        /// <summary>Supports searching for characters in or not in "\0\u0001\u0002\u0003\u0004\u0005\u0006\a\b\u000e\u000f\u0010\u0011\u0012\u0013\u0014\u0015\u0016\u0017\u0018\u0019\u001a\u001b\u001c\u001d\u001e\u001f!\"#$%&amp;'()*+,-./0123456789:;&lt;=&gt;?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~\u007f".</summary>
        internal static readonly SearchValues<char> s_asciiExceptWhiteSpace = SearchValues.Create("\0\u0001\u0002\u0003\u0004\u0005\u0006\a\b\u000e\u000f\u0010\u0011\u0012\u0013\u0014\u0015\u0016\u0017\u0018\u0019\u001a\u001b\u001c\u001d\u001e\u001f!\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~\u007f");
        
        /// <summary>Supports searching for characters in or not in "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz".</summary>
        internal static readonly SearchValues<char> s_asciiLettersAndDigits = SearchValues.Create("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz");
        
        /// <summary>Supports searching for characters in or not in "&amp;&lt;&gt;^|".</summary>
        internal static readonly SearchValues<char> s_ascii_400000500000004000000010 = SearchValues.Create("&<>^|");
        
        /// <summary>Supports searching for characters in or not in "\0\u0001\u0002\u0003\u0004\u0005\u0006\a\b\u000e\u000f\u0010\u0011\u0012\u0013\u0014\u0015\u0016\u0017\u0018\u0019\u001a\u001b\u001c\u001d\u001e\u001f!\"#$%&amp;'()*+,-/0123456789:;&lt;=&gt;?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~\u007f".</summary>
        internal static readonly SearchValues<char> s_ascii_FFC1FFFFFEBFFFFFFFFFFFFFFFFFFFFF = SearchValues.Create("\0\u0001\u0002\u0003\u0004\u0005\u0006\a\b\u000e\u000f\u0010\u0011\u0012\u0013\u0014\u0015\u0016\u0017\u0018\u0019\u001a\u001b\u001c\u001d\u001e\u001f!\"#$%&'()*+,-/0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~\u007f");
    }
}
